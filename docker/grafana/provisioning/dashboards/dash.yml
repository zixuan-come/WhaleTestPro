apiVersion: 1

providers:
  - name: 'whaletestpro'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # Grafana 启动时从这个目录加载 *.json 仪表盘
      path: /var/lib/grafana/dashboards
