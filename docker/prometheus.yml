global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: 'whaletestpro'
    static_configs:
      # compose 里全在同一网络，直接用服务名 app + 容器内端口 8000（不是宿主机的 8001）
      - targets: ['app:8000']
