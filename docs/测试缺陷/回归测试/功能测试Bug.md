# 回归测试 · 功能测试 Bug

> 来源:`docs/bugs.md` 总表按模块拆分的视图。严重度 P0 阻断 / P1 严重 / P2 一般。

| 编号 | 严重度 | 标题 | 现象 | 根因 | 来源 | 状态 |
|------|--------|------|------|------|------|------|
| BUG-032 | P2 | 回归明细展示 DB `case_id`(IDOR/枚举风险) | Regression.vue 明细行 `#{{ r.case_id }}` 直接渲染后端返回的 DB 自增 `case_id`,违反 spec「前端不展示 DB id、改用行序号」(同 BUG-007 家族);且只显示 id 不显示用例名,可读性差 | 明细未做 id 脱敏/行序号化,也未 join 用例名 | 用例设计新发现 | 待修复(前端) |
