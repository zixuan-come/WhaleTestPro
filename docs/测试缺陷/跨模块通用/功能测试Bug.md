# 跨模块通用 · 功能测试 Bug

> 来源:`docs/bugs.md` 总表按模块拆分的视图。跨多个资源页/贯穿全系统、不属单一模块的缺陷。严重度 P0 阻断 / P1 严重 / P2 一般。

| 编号 | 严重度 | 标题 | 现象 | 根因 | 来源 | 状态 |
|------|--------|------|------|------|------|------|
| BUG-003 | P1 | 资源页只有新建+删除,缺编辑 | 建错的记录只能删了重建 | 编辑功能未实现 | task#17 | 已修复 |
| BUG-007 | P1 | 资源列表暴露 DB `id`(IDOR/枚举风险) | 前端直接展示自增 id,可枚举/越权探测 | 列表渲染用了 DB id | 前端·安全 | 已修复 `a7950c2`(改用行序号) |
| BUG-045 | P2 | 回归与场景两条路割裂,不符合 Apifox「场景即运行单元」心智 | `run_regression` 只按 `case_ids`/`tag`/全部跑散用例,走 `run_case`(写报告、算通过率+覆盖率);`scenario` 另起一条路,运行走 `run_chain`(链式提参、但**不写报告**、无 setup/teardown/retries,即 BUG-009)。二者不互通:**场景不能作为回归的运行范围**。**定时调度同源**:`schedule` 到点跑 `run_regression(project_id, tag)`,运行单元同样是"一批用例"而非单接口。演进方向:回归重定位为「接口自动化」,运行单元改为选场景(或场景集/套件),tag 降级为筛选,同步修 BUG-009 | 产品把「回归」与「场景」设计成两条不相干链路;缺「测试套件」概念 | 用例设计新发现(产品对标 Apifox) | 待定(产品演进,关联 BUG-009) |
