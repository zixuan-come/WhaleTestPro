# 场景编排

> PRD v2：按用户功能拆分，供产品确认、Butterfly Agent 评审和测试设计使用。

## 1. 文档信息
| 项目 | 内容 |
| --- | --- |
| 状态 | 评审草案 |
| 目标用户 | 项目成员、链路测试人员 |
| 关联模块 | 测试用例、环境、场景报告 |

## 2. 背景与目标
场景将多个测试用例按业务顺序组织为接口链路，通过共享变量验证跨接口流程，并生成一份场景级报告和逐步骤证据。

## 3. 范围
本期包含场景 CRUD、用例添加/移除/重排、环境选择、变量传递、执行和报告查看；不包含分支、循环、并发和失败即停止。

## 4. 角色与权限
项目成员可维护和执行当前项目场景；项目外场景和用例不可见。

## 5. 功能清单
| ID | 功能 | 优先级 |
| --- | --- | --- |
| SCN-F01 | 创建场景 | P0 |
| SCN-F02 | 编辑场景基本信息 | P1 |
| SCN-F03 | 编排用例步骤 | P0 |
| SCN-F04 | 调整步骤顺序 | P0 |
| SCN-F05 | 配置场景变量传递 | P0 |
| SCN-F06 | 执行场景 | P0 |
| SCN-F07 | 查看场景报告 | P0 |

## 6. 功能需求
### SCN-F01 创建场景
- 用户故事：作为成员，我希望建立业务场景。
- 前置条件：name 非空且不超过 100 字符。
- 主流程：填写 -> 校验项目 -> 创建 -> 返回详情。
- 规则：case_ids 以 JSON 保存，不建数据库外键。
- 验收标准：合法创建 201；空场景可保存；无权项目 404。

### SCN-F02 编辑场景基本信息
- 用户故事：作为维护者，我希望更新名称和描述。
- 规则：描述可空，最大 500 字符；更新不改变步骤顺序。
- 验收标准：合法更新成功；非法字段 422。

### SCN-F03 编排用例步骤
- 用户故事：作为链路测试人员，我希望添加和移除步骤。
- 规则：只能引用当前项目用例；删除用例后历史引用保留，运行时失败。
- 验收标准：保存后重新打开能看到完整 case_ids。

### SCN-F04 调整步骤顺序
- 用户故事：作为维护者，我希望拖拽调整业务顺序。
- 规则：执行严格使用持久化顺序。
- 验收标准：重排后请求顺序与页面顺序一致。

### SCN-F05 配置场景变量传递
- 用户故事：作为维护者，我希望前一步提取值供后续请求使用。
- 规则：提取结果加入共享上下文，支持请求字段和断言；敏感值遮罩。
- 验收标准：后续步骤能读取前一步提取值。

### SCN-F06 执行场景
- 用户故事：作为成员，我希望选择环境执行完整链路。
- 流程：选择环境 -> 初始化上下文 -> 按 case_ids 执行 -> 记录步骤 -> 保存报告。
- 规则：某一步失败后继续；所有步骤通过才算场景通过；空场景生成 0 步报告。
- 验收标准：一次执行只创建一份场景报告；缺失资源步骤失败并记录原因。

### SCN-F07 查看场景报告
- 用户故事：作为排障人员，我希望逐步查看证据。
- 规则：按 sequence 升序展示请求、响应、断言、提取、错误和耗时；响应体最多 64 KiB。
- 验收标准：步骤数量与执行步骤数一致；敏感值不明文展示。

## 7. 非功能要求
报告写入一致；孤立引用可诊断；跨项目访问按 404 处理。

## 8. 当前实现与待确认项
已实现：有序 case_ids、共享上下文、场景报告。
当前限制：复用链路执行，不执行 SQL、数据集和重试；数据库不校验 case_ids。
待确认：分支/循环模型、失败策略、节点级超时。

## 9. 评审记录
v2-draft / 2026-08-29 / 待评审
