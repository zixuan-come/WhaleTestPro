# WhaleTestPro 交接文档（后端测试平台）

> 本文档面向后续接手者（Codex）。目标是让接手者不看历史对话也能明白：这个平台**是什么、已经做到什么程度、还缺什么、下一步怎么走**。
>
> **一句话定位**：WhaleTestPro 是一个**自研接口测试平台**（作品集主线项目），FastAPI 后端 + Vue3 前端，覆盖「接口/用例/场景管理 → 执行引擎 → 断言 → 报告/回归 → 压测 → 流量录制回放」全链路，并接了 Redis/Celery/RabbitMQ/Locust/Prometheus/Grafana，全套 Docker Compose 一键起。
>
> **与 WhaleTestPro-APITest 的关系**：那是一个**独立的黑盒测试仓库**，用来测本平台（本平台是"被测系统/考官"）。两者严格分离，本文档只讲后端平台本身。

---

## 0. 铁律（接手前必读）

1. **分层架构：一个资源 = 一套 5 层文件**（model → schema → repository → service → router），别开"上帝文件"。失控的是单文件太大，不是目录太多。
2. **不改第三方库源码**（venv 里的库不动），要定制走扩展点。
3. **不主动提交**。只有用户明说「提交」时才 `git commit`，精确 `git add`，一天一批续绿点。
4. **不硬编码密钥**。`SECRET_KEY`/`MYSQL_ROOT_PASSWORD` 等走 `.env` → pydantic-settings；非敏感值（如 `REDIS_URL`）可给默认值。`.env` 已 gitignore。
5. **敏感/产物文件永不提交**：`liaotian*.txt`、`grafana-perf.png`、`perf_result_*.csv`、`.env`。

---

## 1. 技术栈与基础设施

| 层 | 技术 |
|----|------|
| Web 框架 | FastAPI（REST 原生：HTTP 状态码 + `detail`） |
| ORM / DB | SQLAlchemy + MySQL 8 |
| 缓存/队列 | Redis（缓存 + JWT 黑名单 + 限流 + Celery result backend） |
| 异步任务 | Celery（broker=RabbitMQ，backend=Redis，各司其职） |
| 压测 | Locust（常驻 master/worker + HTTP 遥控 + `--scale` 动态扩缩） |
| 监控 | Prometheus 埋点（`/metrics`）+ Grafana 仪表盘 |
| 前端 | Vue3（10 个真页面 + 可视化编排 + 双主题） |
| 容器 | Docker Compose（9 服务：app/mysql/redis/rabbitmq/worker/prometheus/grafana/frontend/locust） |

- **App 端口**：容器内 8000，宿主映射 **8001**。注意：执行引擎跑在容器内，env base_url 要用**容器内端口** `http://localhost:8000`。
- **Python 环境**：conda `py310`（`C:\Users\14534\.conda\envs\py310`）。
- 启动：在项目根 `docker compose up -d --build`；Swagger 在 `http://localhost:8001/docs`（已改本地静态资源，不走 CDN）。

---

## 2. 分层架构

```
WhaleTestPro/
├── main.py                 # 入口：注册所有 router、两个中间件（shadow/recording）、Prometheus、/health、/docs
├── app/
│   ├── database.py         # engine + engine_shadow（影子库）、get_db、Base
│   ├── core/               # 横切能力（非资源）
│   │   ├── config.py       # pydantic-settings，从 .env 读
│   │   ├── security.py     # bcrypt 哈希 + JWT 签发/验签
│   │   ├── deps.py         # get_current_user（验签+黑名单+查库）、限流依赖
│   │   ├── redis_client.py / blacklist.py / ratelimit.py  # M1 认证配套
│   │   ├── variables.py    # render(${var}) / extract(JSONPath) —— 接口串联上下文传参
│   │   ├── assertions.py   # 多样化断言（8 种 + json_schema）
│   │   ├── sql_runner.py   # setup/teardown SQL + DB 断言
│   │   ├── circuit_breaker.py # 熔断三态状态机
│   │   ├── shadow_ctx.py   # ContextVar 透传影子标记
│   │   ├── response_diff.py# 流量回放 diff（智能模糊）
│   │   ├── celery_app.py / scheduler.py / metrics.py / notifier.py
│   ├── models/ schemas/ repositories/ services/ routers/  # 每资源一套 5 层
│   └── tasks/              # Celery 任务：perf / traffic / schedule
├── frontend/               # Vue3 前端（见第 6 节）
├── tests/                  # 平台自身的单测（如熔断 test_circuit_breaker.py）
├── docker-compose.yml / Dockerfile / docker/  # 容器编排 + prometheus/grafana 配置
└── .github/workflows/ci.yml # GitHub Actions（跑熔断单测）
```

---

## 3. 已有功能 —— 资源与接口

平台围绕 **project（多项目隔离）** 组织，业务接口需 `Authorization: Bearer <token>` + `X-Project-Id` 双请求头。

| 资源 | 路由前缀 | 端点 | 鉴权 |
|------|----------|------|------|
| 认证 | `/auth` | register / login / logout | 公共 |
| 项目 | `/projects` | POST / GET列表 / GET单个 / DELETE | ✅ 强制 token |
| 接口 | `/interfaces` | CRUD + 分类改名/删除 | ✅ 强制 token |
| 用例 | `/cases` | CRUD + 执行 + **串联 `/cases/chain`** | ⚠️ 无鉴权 |
| 场景 | `/scenarios` | CRUD + 执行 | ✅ 强制 token |
| 环境 | `/environments` | CRUD（多环境管理） | ⚠️ 无鉴权 |
| Mock | `/mock` | CRUD + 动态命中（catch-all） | ⚠️ 无鉴权 |
| 定时任务 | `/schedules` | CRUD（Celery Beat + redbeat 驱动） | ⚠️ 无鉴权 |
| 压测 | `/perf/tasks` | CRUD + `/run` 触发 | ⚠️ 无鉴权 |
| 报告 | `/reports` | 测试报告（通过率等） | — |
| 回归 | `/regression` | 回归门禁（通过率 + 接口覆盖率） | — |
| 流量记录 | `/traffic-records` | 录制的流量 | — |
| 流量回放 | `/traffic-replay` | 回放打影子库 + diff | — |
| demo 订单 | `/demo-order` | 演示用被测业务接口 | — |

（⚠️ 无鉴权的资源是**已知缺陷**，见第 5 节）

---

## 4. 核心能力（平台的"深度"所在）

1. **执行引擎 + 接口串联**：`core/variables.py` 的 `render(${var})` + `extract(JSONPath)` + `services/execution.py` 的 `run_chain`——一条空 `context={}` 贯穿全链，每个用例响应可 `extract_rules` 抠字段进 context，下个用例 url/body 用 `${var}` 引用。`run_chain` 每条 try/continue 隔离（对比 `run_case` 出错即停）。
2. **多样化断言**（`core/assertions.py`）：8 种断言 + JSON Schema 校验。
3. **setup/teardown + DB 断言**（`core/sql_runner.py`）：SQL 直连做前置数据准备 + 结果校验。
4. **参数化/数据驱动**：datasets + `render_deep` 深度渲染。
5. **失败重试 + 测试报告 + 回归门禁**：TestReport 资源 + 通过率/接口覆盖率。
6. **压测（皇冠模块 B）**：常驻 master + N worker、HTTP 遥控、`--scale` 动态扩缩、path 动态广播；worker 进程开 `/metrics`，4 个 perf Gauge 实时曲线。
7. **影子隔离**：打标 `X-Shadow:1` → ContextVar 透传 → `get_bind` 动态选 engine 分流到影子库，压测/回放零污染生产数据。
8. **熔断**：三态状态机（closed/open/half-open）+ 5 个单测全绿，已接入执行引擎两出口。
9. **流量录制回放**（对标 ARex）：中间件录制 → RabbitMQ 缓冲 → worker 落库脱敏 → 回放打影子库 → `response_diff` 智能模糊 diff。关键坑：请求 body 流读一次就没了，中间件里读完必须塞回 `request._body`；响应流迭代耗尽后必须重建 `Response`。
10. **通知告警**：飞书 notifier + notify 开关。
11. **多项目隔离**：所有业务资源带 `project_id`，靠 `X-Project-Id` 头归属。

---

## 5. 已知缺陷 / 缺少的功能

### 5.1 真 BUG（WhaleTestPro-APITest 已用特征化测试钉住，修好后那边会翻红）

| 缺陷 | 现象 | 应有行为 |
|------|------|----------|
| **多资源缺鉴权** | `cases`/`environments`/`mocks`/`schedules`/`perf` 不带 token 也能建（201） | 应挂 `Depends(get_current_user)`，缺 token → 401 |
| **超长 name 崩服务** | name > 255 字符未捕获 → 直接 500（5 类资源一致） | 应 400/422 |
| **非法 cron 崩服务** | schedule 的 cron 传 `not-a-cron` 未校验 → 直接 500 | 应 400/422 |
| **空 name 不校验** | 6 类资源 name 传空串照建（201） | 应 400/422 |
| **perf 并发数不校验** | `users=0` / `users=-5` 也接受（201） | 应 `>=1` |
| **JWT 不唯一** | token 仅含 `sub`+`exp`（整秒），同用户同秒登录得到完全相同 token；注销会误伤同串 token | token 应带 `jti` 唯一标识 |
| **projects/cases 无更新接口** | `/projects`、`/cases` 只有 POST/GET/DELETE，**没有 PUT** | 若需要"改项目/用例"应补 update 端点 |

### 5.2 潜在风险（值得复核）

- **`sql_runner` / DB 断言的 SQL 注入面**：setup/teardown 直接执行用户提供的 SQL，且历史上有 `db_eq` 之类字符串拼接的用法——若这些 SQL 来自不可信输入，存在注入风险。接手者应审计所有拼接 SQL 的地方，改参数化。
- **`run_chain` 无报告落库 / 部分无 try 包裹**：串联执行结果目前 append 到返回值，未沉淀成 TestReport；早期版本 `${var}` 缺失会让整请求 500 中断全链（要更健壮需每用例 try/except 只记该用例 error）。
- **Mock 仅精确匹配**：动态命中是 path 精确匹配，不支持通配/正则/参数匹配。
- **流量回放子调用 Mock 未做**：回放主干①-⑥已完成，被测系统对外部依赖的子调用 Mock 是"进阶留待"。

---

## 6. 前端（Vue3）

- 10 个真页面：接口管理 / 测试用例（含跑测试）/ **场景编排** / 报告 / 回归 / 流量 / 压测 / 环境 / Mock / 调度；侧栏两组入口。
- **可视化编排**：左用例库 + 右可拖拽执行链 + `extract_rules` 变量透传可视化，走后端 `run_chain`（线性，不做假画布分支）。
- 全局 `Modal.vue`（Teleport + 插槽）8 页面复用；JWT 登录 + 双主题 + OPPO 字体。
- `npm run build` 全绿。
- **剩**：浏览器端到端验收（需后端起着）、部署前 OPPO 字体子集化。

---

## 7. 整体进度（29 项主任务表）

沿用固定编号，报进度以此表为准：

- **测试能力线 #1–14**：✅ 全部完成（串联/断言/请求补强/断言深化/setup-teardown/参数化/重试报告/回归门禁/多环境/标签分组/飞书通知/趋势看板/定时调度/Mock）。
- **皇冠 B #15–19**：✅ 全部焊完（分布式压测骨架/实时监控/影子隔离/熔断/流量回放），代码无欠账。
- **架构线（进行中 🔨）**：
  - **#20 前端 C**：主体完成，剩浏览器验收 + 字体子集化。
  - **#21 D（MySQL 调优 + Linux）**：MySQL 索引调优闭环 ✅（EXPLAIN 全流程）；Linux 站①当复习走完；剩 Linux 站②③ + Shell 脚本产物（analyze_log.sh / healthcheck.sh）未落地。
  - **#22 E（K8s + CI/CD）**：CI 半块 ✅（GitHub Actions 跑熔断单测，run#1 Success）；剩 K8s 半块（9 服务 compose → manifest，需 minikube/kind）。
- **#23–26 基础设施**：✅（M1 认证 / 压测模块 / Prometheus+Grafana / Docker 化）。
- **#28 停 docker 全栈**：⬜ 收尾运维（`docker compose down`，临时 demo 库才 `down -v`；开发期不停）。

**注**：熔断的"真实压测前后对比数字"是纯演示验证，计划并入一个独立的 Go 被测系统项目（当"考生"），不挂在本平台待办。

---

## 8. 给接手者（Codex）的建议

1. **加新资源照抄 5 层套路**：model → schema → repository → service → router，别开上帝文件。新 model 记得在 `main.py` import（靠 import 触发 `create_all` 建表）。
2. **给老表加列**：`create_all` 不会给 MySQL 已存在的表补列，要手动 `ALTER TABLE ... ADD COLUMN`；且 Out schema 新字段必须带 `| None` 容忍旧行 NULL。
3. **优先修 5.1 的鉴权缺陷**：给 5 类无鉴权资源挂 `Depends(get_current_user)` 是最高性价比的修复——修完后 WhaleTestPro-APITest 那边对应的特征化用例会翻红，记得同步更新那边的期望值（这是设计好的联动信号）。
4. **审计 SQL 注入面**（5.2），这是唯一的安全隐患方向。
5. **架构线收尾优先级**：#20 浏览器验收（前端已写完，只差跑一遍）→ #22 CI 已有基础可扩 → #21 Shell 脚本 / K8s 偏学习性质，按精力排。
6. **两个项目严格分开**：本仓是被测平台，测试用例在 WhaleTestPro-APITest 那个独立仓；别把黑盒测试代码写进本仓。

---

*文档生成于 2026-07-15。配套的黑盒测试套件说明见 WhaleTestPro-APITest 仓内的交接文档。29 项任务表的实时状态以本文档第 7 节为准。*
